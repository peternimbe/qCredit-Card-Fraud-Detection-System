// Runtime configuration for the browser apps.
// On Render this file is regenerated at container start from the API_URL environment variable
// (see docker/entrypoint.sh). Leave it empty for local development: the pages then fall back to
// http(s)://<current host>:8000.
window.SMARTDETECTOR_API_URL = "";
